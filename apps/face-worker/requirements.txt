# Cmir face-worker — privacy masks on RTSP / files
opencv-python-headless>=4.9.0
numpy>=1.26.0
mediapipe>=0.10.9
imageio>=2.34.0
imageio-ffmpeg>=0.5.0
Pillow>=10.0.0
# SCRFD / InsightFace (small faces, crowds) — optional but recommended for RTSP wide shots
insightface>=0.7.3
onnxruntime>=1.16.0
